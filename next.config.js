/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "http://oaidalleapiprodscus.blob.core.windows.net",
        ]
    }
}

module.exports = nextConfig
